import type { Establishment, EstablishmentContact, SignalLevel } from "./types";
import { fetchWithTimeout } from "./geo.server";

export type VerificationStatus = "verified" | "rejected" | "unverified";
export interface LeadVerification { status: VerificationStatus; score: number; reasons: string[]; checked: boolean; foundDigitalPresence: boolean; foundWebsite: boolean; contactConfidence: "high" | "medium" | "low"; }
type SearchItem = { link?: string; title?: string; snippet?: string };
type SearchResponse = { items: SearchItem[]; ok: boolean };
type VerificationResult = LeadVerification & { contact: EstablishmentContact };
function env(name: string): string | undefined { return typeof process !== "undefined" ? process.env[name]?.trim() || undefined : undefined; }
export function externalVerificationConfigured(): boolean { return Boolean(env("GOOGLE_SEARCH_API_KEY") && env("GOOGLE_SEARCH_CX")); }
function normalize(value: string): string { return value.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-z0-9 ]/g, " ").replace(/\s+/g, " ").trim(); }
function tokens(value: string): string[] { const ignored = new Set(["ltda", "me", "epp", "eireli", "comercio", "empresa", "de", "da", "do", "das", "dos", "e"]); return normalize(value).split(" ").filter((token) => token.length >= 3 && !ignored.has(token)); }
function similarity(name: string, text: string): number { const source = normalize(text); const wanted = tokens(name); if (!wanted.length) return 0; return wanted.filter((token) => source.includes(token)).length / wanted.length; }
function pathIdentity(link: string): string { try { const url = new URL(link); return `${url.hostname} ${url.pathname} ${url.search}`; } catch { return ""; } }
function host(link: string): string { try { return new URL(link).hostname.toLowerCase().replace(/^www\./, ""); } catch { return ""; } }
function isSocial(link: string): boolean { const value = host(link); return ["instagram.com", "facebook.com", "tiktok.com", "youtube.com", "linkedin.com", "x.com", "twitter.com"].some((domain) => value === domain || value.endsWith(`.${domain}`)); }
function isInstagram(link: string): boolean { const value = host(link); return value === "instagram.com" || value.endsWith(".instagram.com"); }
function isWhatsApp(link: string): boolean { const value = host(link); return value === "wa.me" || value === "api.whatsapp.com" || value === "chat.whatsapp.com" || value === "whatsapp.com" || value.endsWith(".whatsapp.com"); }
function isDirectory(link: string): boolean { const value = host(link); return ["tripadvisor.com", "yelp.com", "ifood.com.br", "rappi.com.br", "ubereats.com", "google.com", "google.com.br", "maps.google.com", "wikipedia.org", "wikidata.org", "openstreetmap.org", "goomer.app", "mechamenozap.com.br"].some((domain) => value === domain || value.endsWith(`.${domain}`)); }
function isLikelyOwnWebsite(link: string): boolean { if (!link || isSocial(link) || isDirectory(link) || isWhatsApp(link)) return false; const value = host(link); return Boolean(value && !value.endsWith(".gov.br") && !value.endsWith(".edu.br")); }
function instagramFromLink(link: string): string | null { if (!isInstagram(link)) return null; try { const path = new URL(link).pathname.split("/").filter(Boolean)[0] ?? ""; return /^[A-Za-z0-9_.]{2,}$/.test(path) ? `https://instagram.com/${path}` : null; } catch { return null; } }
function whatsappFromLink(link: string): string | null { try { const url = new URL(link); if (!isWhatsApp(link)) return null; if (url.hostname === "wa.me") { const digits = url.pathname.replace(/\D/g, ""); return digits.length >= 11 && digits.length <= 15 ? `https://wa.me/${digits}` : null; } const phone = url.searchParams.get("phone")?.replace(/\D/g, "") ?? ""; return phone.length >= 11 && phone.length <= 15 ? `https://wa.me/${phone}` : null; } catch { return null; } }
async function googleSearch(query: string): Promise<SearchResponse> { const key = env("GOOGLE_SEARCH_API_KEY"); const cx = env("GOOGLE_SEARCH_CX"); if (!key || !cx) return { items: [], ok: false }; const url = new URL("https://customsearch.googleapis.com/customsearch/v1"); url.searchParams.set("key", key); url.searchParams.set("cx", cx); url.searchParams.set("q", query); url.searchParams.set("num", "8"); url.searchParams.set("hl", "pt-BR"); url.searchParams.set("gl", "br"); try { const response = await fetchWithTimeout(url.toString(), { headers: { Accept: "application/json" } }, 5000); if (!response.ok) return { items: [], ok: false }; const data = (await response.json()) as { items?: SearchItem[] }; return { items: data.items ?? [], ok: true }; } catch { return { items: [], ok: false }; } }
async function searchLeadPresence(lead: Establishment): Promise<{ items: SearchItem[]; successfulQueries: number }> { const cleanName = lead.name.replace(/["']/g, " ").replace(/\s+/g, " ").trim(); const location = [lead.details.neighbourhood, lead.details.city, lead.details.state].filter(Boolean).join(" "); const address = [lead.details.street, lead.details.housenumber].filter(Boolean).join(" "); const phone = lead.contact.phoneDigits ?? ""; const identity = [`"${cleanName}"`, location ? `"${location}"` : "", address ? `"${address}"` : ""].filter(Boolean).join(" "); const query = phone ? `${identity} "${phone}" (site:instagram.com OR site:wa.me OR whatsapp)` : `${identity} (site:instagram.com OR site:wa.me OR whatsapp OR site:tiktok.com OR site:facebook.com)`; const response = await googleSearch(query); return { items: response.items, successfulQueries: Number(response.ok) }; }
function contactConfidence(contact: EstablishmentContact): "high" | "medium" | "low" { if (contact.whatsappValid || contact.email) return "high"; if (contact.phoneDigits || contact.instagramUrl || contact.facebookUrl) return "medium"; return "low"; }
function evidenceScore(lead: Establishment, item: SearchItem): { identity: number; context: number } { const evidence = `${item.title ?? ""} ${item.snippet ?? ""}`; const urlEvidence = pathIdentity(item.link ?? ""); const locationText = [lead.details.neighbourhood, lead.details.city, lead.details.state].filter(Boolean).join(" "); const identity = Math.max(similarity(lead.name, evidence), similarity(lead.name, urlEvidence)); const context = Math.max(locationText ? similarity(locationText, evidence) : 0, lead.contact.phoneDigits && normalize(evidence).includes(normalize(lead.contact.phoneDigits)) ? 1 : 0, lead.contact.email && normalize(evidence).includes(normalize(lead.contact.email)) ? 1 : 0); return { identity, context }; }
export async function verifyLead(lead: Establishment): Promise<VerificationResult> { const existingWebsite = Boolean(lead.signals.website || lead.contact.websiteUrl); const existingInstagram = Boolean(lead.signals.instagram || lead.contact.instagramUrl); const existingWhatsApp = Boolean(lead.contact.whatsappValid); const initialContact: EstablishmentContact = { ...lead.contact }; const confidence = contactConfidence(initialContact); const empty: LeadVerification = { status: "unverified", score: 0, reasons: ["Verificação web indisponível: configure GOOGLE_SEARCH_API_KEY e GOOGLE_SEARCH_CX no Render."], checked: false, foundDigitalPresence: existingWebsite || existingInstagram || existingWhatsApp, foundWebsite: existingWebsite, contactConfidence: confidence }; if (!externalVerificationConfigured()) return { ...empty, contact: initialContact }; const search = await searchLeadPresence(lead); if (search.successfulQueries === 0) return { status: "unverified", score: 0, reasons: ["A busca web não respondeu. O lead foi mantido sem inferir ausência de presença digital."], checked: false, foundDigitalPresence: existingWebsite || existingInstagram || existingWhatsApp, foundWebsite: existingWebsite, contactConfidence: confidence, contact: initialContact }; let foundWebsite = existingWebsite; let foundInstagram = existingInstagram; let foundWhatsApp = existingWhatsApp; const contact: EstablishmentContact = { ...initialContact }; const reasons: string[] = []; for (const item of search.items) { const link = item.link ?? ""; if (!link) continue; const { identity, context } = evidenceScore(lead, item); const identityStrong = identity >= 0.75 || (tokens(lead.name).length <= 2 && identity >= 1); const identityVeryStrong = identity >= 0.9; const hasLocationData = Boolean(lead.details.neighbourhood || lead.details.city || lead.details.state || lead.contact.phoneDigits || lead.contact.email); const contextual = context >= 0.25 || (lead.details.city ? normalize(item.title ?? "").includes(normalize(lead.details.city)) : false) || (!hasLocationData && identityVeryStrong) || lead.knownBrand; if (!identityStrong || !contextual) continue; const instagramUrl = instagramFromLink(link); if (instagramUrl) { foundInstagram = true; contact.instagramUrl = instagramUrl; try { const handle = new URL(instagramUrl).pathname.split("/").filter(Boolean)[0] ?? ""; contact.instagramHandle = handle ? `@${handle}` : contact.instagramHandle; } catch {} reasons.push("Instagram encontrado pela busca web com correspondência de identidade e contexto."); continue; } const whatsappUrl = whatsappFromLink(link); if (whatsappUrl) { foundWhatsApp = true; contact.whatsappUrl = whatsappUrl; contact.whatsappValid = true; reasons.push("WhatsApp encontrado pela busca web com correspondência de identidade e contexto."); continue; } if (isLikelyOwnWebsite(link)) { foundWebsite = true; contact.websiteUrl = link; reasons.push("Site próprio encontrado pela busca web com correspondência de identidade e contexto."); } } const signalCount = Number(foundWebsite) + Number(foundInstagram) + Number(foundWhatsApp); const foundDigitalPresence = signalCount > 0; const nextContactConfidence = contactConfidence(contact); const status: VerificationStatus = foundWebsite ? "rejected" : "verified"; const score = foundWebsite ? 0 : 100; const finalReasons = [...new Set([...reasons, foundWebsite ? "O estabelecimento possui site próprio confirmado pela busca web." : foundDigitalPresence ? "Foi encontrada presença digital relevante para os critérios de Sinal Zero." : "Nenhuma presença digital relevante para site, Instagram ou WhatsApp foi confirmada pela busca web."])]; return { status, score, reasons: finalReasons, checked: true, foundDigitalPresence, foundWebsite, contactConfidence: nextContactConfidence, contact }; }
function unverifiedResult(lead: Establishment): Establishment & { verification: LeadVerification } { return { ...lead, verification: { status: "unverified", score: 0, reasons: ["Verificação externa não executada neste lote para manter a pesquisa rápida."], checked: false, foundDigitalPresence: Boolean(lead.signals.website || lead.signals.instagram || lead.contact.whatsappValid || lead.contact.instagramUrl), foundWebsite: Boolean(lead.signals.website || lead.contact.websiteUrl), contactConfidence: contactConfidence(lead.contact) } }; }
export async function verifyLeads(leads: Establishment[]): Promise<(Establishment & { verification: LeadVerification })[]> { const maxToVerify = 40; const concurrency = 10; const candidates = leads.slice(0, maxToVerify); const output: (Establishment & { verification: LeadVerification })[] = []; for (let i = 0; i < candidates.length; i += concurrency) { const batch = candidates.slice(i, i + concurrency); const verified = await Promise.all(batch.map(async (lead) => { const verification = await verifyLead(lead); const contact = verification.contact; const website = Boolean(lead.signals.website || contact.websiteUrl || verification.foundWebsite); const instagram = Boolean(lead.signals.instagram || contact.instagramUrl); const whatsapp = Boolean(contact.whatsappValid); const rawSignalCount = Number(website) + Number(instagram) + Number(whatsapp); const signalCount = lead.knownBrand ? Math.max(rawSignalCount, 3) : rawSignalCount; const level: SignalLevel = signalCount >= 2 ? "full" : signalCount === 1 ? "weak" : "zero"; return { ...lead, contact, contactable: Boolean(contact.whatsappValid || contact.instagramUrl), signals: { ...lead.signals, website, instagram }, signalCount, level, verification: { status: verification.status, score: verification.score, reasons: verification.reasons, checked: verification.checked, foundDigitalPresence: verification.foundDigitalPresence, foundWebsite: verification.foundWebsite, contactConfidence: verification.contactConfidence } }; })); output.push(...verified); } output.push(...leads.slice(maxToVerify).map(unverifiedResult)); return output; }
