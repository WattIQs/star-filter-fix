import { useEffect, useRef, useState } from "react";
import { Loader2, MapPin, Search } from "lucide-react";
import { useServerFn } from "@tanstack/react-start";
import { searchPlacesServer, type PlaceSuggestion } from "@/lib/geo.functions";
import { cn } from "@/lib/utils";
interface PlaceSearchBarProps { onPick:(place:PlaceSuggestion)=>void; scanning:boolean; currentLabel:string|null; }
export function PlaceSearchBar({onPick,scanning,currentLabel}:PlaceSearchBarProps){const searchPlaces=useServerFn(searchPlacesServer);const [value,setValue]=useState("");const [suggestions,setSuggestions]=useState<PlaceSuggestion[]>([]);const [open,setOpen]=useState(false);const [loading,setLoading]=useState(false);const [highlight,setHighlight]=useState(0);const boxRef=useRef<HTMLDivElement>(null);const requestIdRef=useRef(0);const pickedLabelRef=useRef<string|null>(null);useEffect(()=>{if(pickedLabelRef.current!==null&&value===pickedLabelRef.current)return;const term=value.trim();if(term.length<2){setSuggestions([]);setOpen(false);setLoading(false);return;}const id=++requestIdRef.current;let cancelled=false;setLoading(true);const timer=setTimeout(()=>{searchPlaces({data:{q:term}}).then(list=>{if(cancelled||id!==requestIdRef.current)return;setSuggestions(list);setHighlight(0);setOpen(list.length>0);}).catch(()=>{if(!cancelled&&id===requestIdRef.current){setSuggestions([]);setOpen(false);}}).finally(()=>{if(!cancelled&&id===requestIdRef.current)setLoading(false);});},260);return()=>{cancelled=true;clearTimeout(timer);};},[value,searchPlaces]);useEffect(()=>{const onClick=(e:MouseEvent)=>{if(!boxRef.current?.contains(e.target as Node))setOpen(false);};document.addEventListener("mousedown",onClick);return()=>document.removeEventListener("mousedown",onClick);},[]);const pick=(place:PlaceSuggestion)=>{pickedLabelRef.current=place.shortLabel;setSuggestions([]);setValue(place.shortLabel);setOpen(false);onPick(place);};return <div ref={boxRef} className="relative min-w-0 flex-1"><span className="pointer-events-none absolute left-3 top-1/2 z-10 -translate-y-1/2 text-muted-foreground">{scanning||loading?<Loader2 className="h-3.5 w-3.5 animate-spin"/>:<Search className="h-3.5 w-3.5"/>}</span><input value={value} onChange={e=>{pickedLabelRef.current=null;setValue(e.target.value);}} onFocus={()=>suggestions.length>0&&setOpen(true)} onKeyDown={e=>{if(!open||!suggestions.length){if(e.key==="Escape")setOpen(false);return;}if(e.key==="ArrowDown"){e.preventDefault();setHighlight(h=>(h+1)%suggestions.length);}else if(e.key==="ArrowUp"){e.preventDefault();setHighlight(h=>(h-1+suggestions.length)%suggestions.length);}else if(e.key==="Enter"){e.preventDefault();const p=suggestions[highlight];if(p)pick(p);}else if(e.key==="Escape")setOpen(false);}} placeholder={currentLabel??"Buscar cidade, estado, bairro ou endereço"} aria-label="Buscar lugar no mapa" autoComplete="off" inputMode="search" className="h-10 w-full rounded-full border border-border bg-background/95 pl-9 pr-3 text-[13px] outline-none transition-[border-color,box-shadow] duration-200 placeholder:text-muted-foreground/70 focus:border-primary focus:ring-4 focus:ring-primary/10 sm:h-9 sm:text-xs"/>{open&&value!==pickedLabelRef.current&&suggestions.length>0&&<div className="absolute left-0 right-0 top-11 z-[900] origin-top overflow-hidden rounded-2xl border border-border bg-popover/98 shadow-2xl backdrop-blur-md animate-in fade-in-0 zoom-in-95 duration-150"><div className="border-b border-border/60 px-3 py-2 text-[10px] font-medium text-muted-foreground">Selecione o local correto antes de varrer</div><ul className="max-h-[min(18rem,55vh)] overflow-y-auto overscroll-contain">{suggestions.map((place,i)=><li key={`${place.lat}-${place.lon}-${i}`}><button type="button" onMouseEnter={()=>setHighlight(i)} onClick={()=>pick(place)} className={cn("flex min-h-12 w-full items-start gap-2 px-3 py-2.5 text-left transition-all duration-100 active:scale-[.99]",i===highlight?"bg-muted":"hover:bg-muted/60")}><MapPin className="mt-0.5 h-4 w-4 shrink-0 text-primary"/><span className="min-w-0"><span className="block truncate text-xs font-semibold text-foreground sm:text-sm">{place.shortLabel}</span><span className="mt-0.5 block line-clamp-2 text-[10px] leading-4 text-muted-foreground sm:text-[11px]">{place.label}</span></span></button></li>)}</ul></div>}</div>}
