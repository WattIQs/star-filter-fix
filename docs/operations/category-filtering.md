# Category filtering

Category matching must use normalized, explicit category rules. Substrings that merely contain another category name must not qualify as an exact category match; for example, `barbearia` must not satisfy the `bares` category.
